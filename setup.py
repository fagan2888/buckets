from setuptools import find_packages, setup

setup(name='buckets',
      version='0.01',
      description='Better binning.',
      author='Robert Yi',
      author_email='root@ryi.me',
      packages=find_packages(),
      zip_safe=False)
